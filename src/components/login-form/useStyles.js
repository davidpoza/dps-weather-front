import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  createAccount: {
    padding: 0,
    fontSize: '0.9em',
    '&:hover': {
      backgroundColor: 'transparent',
    },
  },
}));
